# Optional local AI tooling.
# Copy to `.env` only when the user wants local tool configuration.

# Context7 API key for local CLI/MCP usage.
CONTEXT7_API_KEY=

# Optional label for local usage reports.
TOKSCALE_PROJECT=AGENTS
